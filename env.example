# Ambiente Perfumix
# Renomeie para .env e ajuste os valores

# Backend/API
PORT=4000
JWT_SECRET=troque-esta-chave-secreta
# DB_URL pode ser Postgres (prod) ou SQLite (dev)
# Postgres (Docker Compose):
DB_URL=postgresql://perfumix:perfumix@db:5432/perfumix
# SQLite (dev, local):
# DB_URL=file:./dev.db

# Pagamentos
PAYMENT_ENV=sandbox
PAYMENT_PIX_KEY=
PAYMENT_CARD_KEY=
PAYMENT_BOLETO_KEY=
MOCK_PAYMENT_REDIRECT=https://example.com/pagamento-simulado

# Netlify (Frontend)
# Caso precise expor a URL da API para o front via env:
# VITE_PERFUMIX_API=https://sua-api.exemplo.com
